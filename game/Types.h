//---------------------------------------------------------------------------

#ifndef TypesH
#define TypesH

enum moveResults {
	HIT,
	KILL,
	MISS,
	ERR
};

enum gameTypes {
	ONLINE,
	COMPUTER
};

enum gameStatuses {
	RUNNING,
    STOPPED
};

enum moveStatuses {
	MINE,
    ENEMY
};
//---------------------------------------------------------------------------
#endif
